# Positive Patterns

Store reusable examples of what good looks like.

Possible contents:
- strong implementation plans
- clear migration plans
- robust review checklists
- good room context files

Use the default naming convention:
`[topic]__[type]__[status]__v[version].md`
